package com.kihlberg.framework.astronomy;

import junit.framework.TestCase;

import java.util.Calendar;
import java.util.TimeZone;

public class AstronomyProviderTest extends TestCase {

/*
    public void testGetSunPosition() throws Exception {
        ITimeProvider timeProvider = new MockTimeProvider();
        AstronomyProvider a = new AstronomyProvider(timeProvider);
        SunPosition position = a.GetSunPosition();

    }

    private class MockTimeProvider implements ITimeProvider {
        @Override
        public Calendar GetTime() {
            Calendar cal = Calendar.getInstance(TimeZone.getTimeZone("UTC"));
            cal.set(2015,3,25,14,00,00);
            return cal;
        }
    }
    */
}